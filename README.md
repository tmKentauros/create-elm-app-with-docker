# create-elm-app-with-docker

Example of an elm project using create-elm-app, scss, docker-compose, and more.

## Initialize

```cmd
docker-compose build
```

## Debug elm with development server

```cmd
docker-compose up
```

## Build

Show `.travis.yml`.
